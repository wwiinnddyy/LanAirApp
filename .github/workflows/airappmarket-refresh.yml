name: Refresh AirAppMarket Index

on:
  workflow_dispatch:
  repository_dispatch:
    types:
      - official-plugin-released
  push:
    branches:
      - main
    paths:
      - "airappmarket/registry/**"
      - ".github/workflows/airappmarket-refresh.yml"

permissions:
  contents: write

concurrency:
  group: airappmarket-refresh-${{ github.ref }}
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "10.0.x"

      - name: Regenerate official market index
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          dotnet run --project airappmarket\tools\AirAppMarket.IndexBuilder\AirAppMarket.IndexBuilder.csproj -- --registry airappmarket\registry\official-plugins.json --validate-registry-only
          dotnet run --project airappmarket\tools\AirAppMarket.IndexBuilder\AirAppMarket.IndexBuilder.csproj -- --validate-market-manifest airappmarket\templates\market-manifest.template.json
          dotnet run --project airappmarket\tools\AirAppMarket.IndexBuilder\AirAppMarket.IndexBuilder.csproj -- --self-test
          dotnet run --project airappmarket\tools\AirAppMarket.IndexBuilder\AirAppMarket.IndexBuilder.csproj -- --registry airappmarket\registry\official-plugins.json --output airappmarket\index.json --require-market-manifest

      - name: Validate generated index
        shell: pwsh
        run: |
          dotnet run --project airappmarket\tools\AirAppMarket.Validator\AirAppMarket.Validator.csproj -- --self-test --expected-api-version 5.0.0
          dotnet run --project airappmarket\tools\AirAppMarket.Validator\AirAppMarket.Validator.csproj -- airappmarket\index.json airappmarket\schema\airappmarket-index.schema.json --expected-api-version 5.0.0

      - name: Commit refreshed index
        shell: pwsh
        run: |
          $status = git status --porcelain -- airappmarket/index.json
          if ([string]::IsNullOrWhiteSpace($status)) {
            Write-Host "Official market index is already up to date."
            exit 0
          }

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add airappmarket/index.json
          git commit -m "chore: refresh official market index"
          git push origin HEAD:main
